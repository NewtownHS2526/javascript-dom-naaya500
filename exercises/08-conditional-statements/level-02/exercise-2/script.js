// TODO: Check multiple conditions with ||
// Example:
// if (day === "Saturday" || day === "Sunday") {
//     console.log("Weekend!");
// }
let day = "Saturday";
if (day === "Saturday" || day === "Sunday") {
    console.log("Weekend!");
}
